# SOS - Система генерации тестов Allure TestOps

Система для автоматической генерации тест-кейсов в формате Allure TestOps as Code с использованием LLM.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск](#запуск)
- [Использование](#использование)
- [Переменные окружения](#переменные-окружения)

## 🎯 Описание

SOS - это веб-приложение для генерации тест-кейсов в формате Allure TestOps as Code. Система поддерживает несколько режимов работы:

- **Green** - Генерация тест-кейсов из текстовых требований
- **Lime** - Генерация автоматизированных тестов из OpenAPI спецификации
- **Blue** - Оптимизация существующих тест-кейсов
- **Purple** - Проверка тест-кейсов на соответствие стандартам

## ✨ Возможности

- Генерация тест-кейсов из текстовых требований
- Генерация автоматизированных тестов из OpenAPI спецификаций
- Оптимизация существующих тест-кейсов
- Проверка соответствия стандартам Allure TestOps
- Поддержка паттерна AAA (Arrange-Act-Assert)
- Веб-интерфейс для удобной работы
- Docker контейнеризация для простого развертывания

## 📦 Требования

- Docker и Docker Compose (для запуска через Docker)
- ИЛИ Python 3.11+ и Node.js 20+ (для локального запуска)
- OpenAI API ключ (или совместимый API)

## 🚀 Установка

### Вариант 1: Установка через Docker (Рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd SOS
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` файл и укажите ваши значения:
```env
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://foundation-models.api.cloud.ru/v1
```

4. Запустите приложение:
```bash
docker-compose up -d
```

### Вариант 2: Локальная установка

#### Установка сервера

1. Перейдите в директорию сервера:
```bash
cd server
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

#### Установка клиента

1. Перейдите в директорию клиента:
```bash
cd client
```

2. Установите зависимости:
```bash
npm install
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://foundation-models.api.cloud.ru/v1

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Client API Endpoints (для локального запуска)
ALLURE_GENERATOR_ENDPOINT=http://localhost:8000/generate
API_ENDPOINT_GREEN=http://localhost:8000/generate
API_ENDPOINT_LIME=http://localhost:8000/lime
API_ENDPOINT_BLUE=http://localhost:8000/blue
API_ENDPOINT_PURPLE=http://localhost:8000/purple

# Docker Configuration (автоматически используется в docker-compose)
NEXT_PUBLIC_API_URL=http://server:8000
```

### Описание переменных

- `OPENAI_API_KEY` - API ключ для доступа к OpenAI API (обязательно)
- `OPENAI_BASE_URL` - Базовый URL для OpenAI API (обязательно)
- `SERVER_HOST` - Хост для сервера (по умолчанию: 0.0.0.0)
- `SERVER_PORT` - Порт для сервера (по умолчанию: 8000)
- `ALLURE_GENERATOR_ENDPOINT` - Эндпоинт для генерации тестов (для клиента)
- `API_ENDPOINT_*` - Эндпоинты для различных режимов работы

## 🏃 Запуск

### Запуск через Docker

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

После запуска:
- Клиент будет доступен по адресу: http://localhost:3000
- Сервер API будет доступен по адресу: http://localhost:8000

### Локальный запуск

#### Запуск сервера

```bash
cd server
source venv/bin/activate  # На Windows: venv\Scripts\activate
uvicorn main:app --host 0.0.0.0 --port 8000
```

#### Запуск клиента

В новом терминале:
```bash
cd client
npm run dev
```

## 📖 Использование

1. Откройте веб-интерфейс по адресу http://localhost:3000
2. Выберите режим работы (Green, Lime, Blue, Purple)
3. Введите данные:
   - **Green**: Текстовые требования для генерации тест-кейсов
   - **Lime**: OpenAPI спецификация в формате YAML или JSON
   - **Blue**: Существующий код тест-кейсов для оптимизации
   - **Purple**: Существующий код тест-кейсов для проверки
4. Нажмите кнопку отправки и дождитесь результата
5. Скопируйте сгенерированный код

## 🔧 Примеры использования

### Пример 1: Генерация тест-кейсов (Green)

Введите текстовые требования:
```
Создай тест-кейсы для проверки авторизации пользователя:
- Проверка входа с валидными данными
- Проверка входа с невалидным паролем
- Проверка входа с несуществующим пользователем
```

### Пример 2: Генерация из OpenAPI (Lime)

Вставьте OpenAPI спецификацию:
```yaml
openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /users:
    get:
      summary: Получить список пользователей
      responses:
        '200':
          description: Успешный ответ
```

### Пример 3: Оптимизация тестов (Blue)

Вставьте существующий код тест-кейсов для оптимизации.

### Пример 4: Проверка стандартов (Purple)

Вставьте код тест-кейсов для проверки на соответствие стандартам Allure TestOps.

## 🐳 Docker команды

```bash
# Пересборка образов
docker-compose build

# Пересборка и запуск
docker-compose up -d --build

# Просмотр логов сервера
docker-compose logs -f server

# Просмотр логов клиента
docker-compose logs -f client

# Остановка и удаление контейнеров
docker-compose down

# Остановка и удаление контейнеров с volumes
docker-compose down -v
```

## 🔍 Проверка работоспособности

Проверьте здоровье сервера:
```bash
curl http://localhost:8000/health
```

Ожидаемый ответ:
```json
{"status": "ok"}
```

## 📝 Структура проекта

```
SOS/
├── server/              # FastAPI сервер
│   ├── main.py         # Основной файл сервера
│   ├── schemas/        # Pydantic схемы
│   ├── Dockerfile      # Dockerfile для сервера
│   └── requirements.txt
├── client/             # Next.js клиент
│   ├── app/            # Next.js приложение
│   ├── Dockerfile      # Dockerfile для клиента
│   └── package.json
├── docker-compose.yml  # Docker Compose конфигурация
├── .env.example        # Пример файла с переменными окружения
└── README.md           # Документация
```

## 🛠️ Разработка

### Добавление новых зависимостей

**Сервер:**
```bash
cd server
pip install <package>
pip freeze > requirements.txt
```

**Клиент:**
```bash
cd client
npm install <package>
```

## ⚠️ Важные замечания

1. **API ключ**: Никогда не коммитьте файл `.env` с реальными ключами в репозиторий
2. **Безопасность**: В продакшене измените CORS настройки в `server/main.py`
3. **Производительность**: Для больших запросов увеличьте лимиты в конфигурации

## 📄 Лицензия

[Укажите лицензию]

## 👥 Авторы

[Укажите авторов]

## 🤝 Вклад

[Укажите информацию о вкладе в проект]
