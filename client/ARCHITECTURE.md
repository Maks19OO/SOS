# Архитектура приложения

## 📁 Структура проекта

```
app/
├── components/          # Переиспользуемые UI компоненты
│   ├── ColorModeSelector.tsx  # Селектор цветовых режимов (трапеция)
│   ├── Sidebar.tsx           # Левая панель с историей чатов
│   ├── ChatArea.tsx          # Основная область чата
│   ├── MessageInput.tsx      # Компонент ввода сообщений
│   └── MessageList.tsx       # Список сообщений с разделением на колонки
│
├── hooks/               # Пользовательские хуки
│   └── useChat.ts       # Хук для управления состоянием чата
│
├── types/               # TypeScript типы и интерфейсы
│   └── chat.ts          # Типы для чатов, сообщений, цветовых режимов
│
├── constants/           # Константы и конфигурация
│   └── colorModes.ts    # Конфигурация цветовых режимов и моковые данные
│
├── providers/           # React Context провайдеры
│   └── SnackThemeProvider.tsx  # Провайдер темы и цветовых режимов
│
└── page.tsx            # Главная страница (композиция компонентов)
```

## 🏗️ Архитектурные принципы

### 1. **Разделение ответственности (SoC)**
Каждый компонент отвечает за одну конкретную задачу:
- `ColorModeSelector` - только выбор цвета
- `Sidebar` - только навигация по чатам
- `MessageInput` - только ввод сообщений
- `MessageList` - только отображение истории

### 2. **Custom Hooks для бизнес-логики**
Вся логика работы с чатами инкапсулирована в `useChat` хук:
- Управление состоянием (chats, messages, activeChat)
- Обработчики событий (handleSendMessage, handleNewChat)
- Логика ввода с клавиатуры

### 3. **Типизация**
Строгая типизация через TypeScript:
```typescript
type Chat = { id: number; title: string; timestamp: string };
type Message = { text: string; response: string };
enum ColorMode { Green, Lime, Blue, Purple }
```

### 4. **Константы**
Вынесение конфигурации в отдельные файлы:
- Цветовые палитры
- Моковые данные
- Конфигурация режимов

### 5. **Композиция компонентов**
Главная страница (`page.tsx`) - это композиция маленьких компонентов:
```tsx
<App>
  <ColorModeSelector />
  <Sidebar />
  <ChatArea>
    <MessageInput />
    <MessageList />
  </ChatArea>
</App>
```

## 🔄 Поток данных

```
page.tsx (главный контроллер)
    ↓
useChat hook (состояние и логика)
    ↓
Components (отображение и события)
    ↓
Callbacks (обновление состояния)
```

## 🎨 Система стилей

- Использование CSS переменных для тем
- Inline styles для динамических значений
- Переменные цветовых режимов через data-атрибуты

## 🧩 Компоненты

### ColorModeSelector
**Назначение**: Выбор цветовой темы  
**Props**: `colorMode`, `onColorModeChange`  
**Особенности**: Абсолютное позиционирование, форма трапеции

### Sidebar
**Назначение**: Навигация по истории чатов  
**Props**: `chats`, `activeChat`, `onChatSelect`, `onNewChat`  
**Особенности**: Фиксированная ширина, скроллируемый список

### ChatArea
**Назначение**: Основная область работы с сообщениями  
**Props**: `messages`, `inputValue`, `onInputChange`, `onSendMessage`, `onKeyPress`  
**Особенности**: Два режима - пустой экран и список сообщений

### MessageInput
**Назначение**: Ввод текста сообщения  
**Props**: `value`, `onChange`, `onSend`, `onKeyPress`  
**Особенности**: Автоматический рост, кнопка отправки, акцентная тень

### MessageList
**Назначение**: Отображение истории сообщений  
**Props**: `messages`  
**Особенности**: Grid layout (50/50), разные стили для запроса и ответа

## 🪝 Custom Hook: useChat

**Состояние**:
- `chats` - список всех чатов
- `activeChat` - ID активного чата
- `inputValue` - текущий текст ввода
- `messages` - сообщения в текущем чате

**Методы**:
- `handleSendMessage()` - отправка сообщения
- `handleNewChat()` - создание нового чата
- `handleKeyPress()` - обработка Enter/Shift+Enter

## 🎨 Цветовые режимы

4 режима с динамическими акцентными цветами:
1. **Green** (#10b981) - Зелёный (основной)
2. **Lime** (#84cc16) - Лайм
3. **Blue** (#3b82f6) - Синий
4. **Purple** (#a855f7) - Фиолетовый

Система CSS переменных:
```css
--accent-primary
--accent-hover
--accent-light
```

## 🚀 Преимущества архитектуры

✅ **Масштабируемость** - легко добавлять новые компоненты  
✅ **Переиспользование** - компоненты независимы  
✅ **Тестируемость** - изолированная логика в хуках  
✅ **Читаемость** - чистый и понятный код  
✅ **Типобезопасность** - TypeScript предотвращает ошибки  
✅ **Производительность** - оптимизированный рендеринг  

## 📦 Зависимости

- `React` - UI библиотека
- `Next.js` - фреймворк
- `@snack-uikit/*` - UI компоненты
- `TypeScript` - типизация

